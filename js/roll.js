function roll(num) {
	var return = Math.floor(Math.random(1, num) * 19 + 1); 
	// Math.floor rounds the number down to the nearest integer
	return return;
}

function roll2(num) {
	console.log(parseInt(num));
	var return = Math.floor(Math.random(1, num) * 19 + 1); 
	// Math.floor rounds the number down to the nearest integer
	return return;
}